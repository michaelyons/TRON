const { assert } = require('chai');
const Game = require('../lib/Game.js');

describe ('Game', function () {

  it('', function() {
    assert.deepEqual(, {

    })
  });

  it('', function() {
    assert.deepEqual(, {
      
    })
  });

  it('', function() {
    assert.deepEqual(, {
      
    })
  });

  it('', function() {
    assert.deepEqual(, {
      
    })
  });

  it('', function() {
    assert.deepEqual(, {
      
    })
  });
})